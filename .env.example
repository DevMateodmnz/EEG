# Optional local data/cache location for MNE-compatible acquisition.
# Do not commit raw EEG, credentials, or provider tokens.
MNE_DATA=/absolute/path/to/eeg-data

# Used only by the ongoing Lee2019 acquisition command. Valid values: auto, nemar, upstream.
MOABB_DOWNLOAD_PROVIDER=nemar
